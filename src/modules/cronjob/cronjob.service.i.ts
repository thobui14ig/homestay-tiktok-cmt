export interface IGetInfoFromTiktok {
    title: string,
    author_name: string,
    embed_product_id: string
}

export interface ICommentFromTiktok {
    commentId: string;
    userNameComment: string;
    commentMessage: string;
    phoneNumber: string;
    userIdComment: string;
    commentCreatedAt: string;
    postId: string
}